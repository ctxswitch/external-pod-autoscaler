apiVersion: v1
kind: Pod
metadata:
  name: worker-pod-1
  namespace: default
  labels:
    app: worker
spec:
  containers:
  - name: worker
    image: worker:latest
status:
  phase: Running
  podIP: "10.0.0.100"
  conditions:
  - type: Ready
    status: "True"
